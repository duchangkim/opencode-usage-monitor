# ===========================================
# Agentic Usage Monitor - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# -------------------------------------------
# CONFIGURATION
# -------------------------------------------

# Refresh Interval (Optional)
# How often to fetch new data, in seconds
# Default: 30
# Minimum: 10 (to avoid rate limiting)
USAGE_MONITOR_REFRESH_INTERVAL=30

# tmux Session Name (Optional)
# Name for the tmux session when using opencode-with-monitor
# Default: opencode
USAGE_MONITOR_SESSION=opencode

# Monitor Pane Width (Optional)
# Width of the monitor pane as percentage of terminal width
# Default: 25
# Range: 15-50 recommended
USAGE_MONITOR_WIDTH=25

# -------------------------------------------
# AUTHENTICATION (Auto-detected)
# -------------------------------------------
# The monitor automatically loads OAuth credentials from:
#
# 1. OpenCode: ~/.local/share/opencode/auth.json
#    - Run: opencode auth login
#
# 2. Claude Code: ~/.claude/.credentials.json
#    - Run: claude (first-time setup will prompt login)
#
# No manual configuration needed for OAuth.

# -------------------------------------------
# TEST / DEVELOPMENT SETTINGS
# -------------------------------------------
# These are only needed when running E2E tests

# Test Credentials Path (Testing only)
# Path to mock credentials file for E2E testing
# TEST_CREDENTIALS_PATH=./test/fixtures/credentials.json

# OAuth API Base URL (Testing only)
# Override OAuth API endpoint for mock server testing
# OAUTH_API_BASE=http://localhost:8765/api/oauth

# Mock Server Port (Testing only)
# Port for the mock OAuth server
# PORT=8765

# Mock Server Scenario (Testing only)
# Available: healthy, lowUsage, highUsage, rateLimited,
#            authError, enterpriseOrg, noLimits, slowResponse, serverError
# SCENARIO=healthy

# Use Real API in Tests (Testing only)
# Set to 'true' to run smoke tests against real API
# USE_REAL_API=false

# Docker Environment Flag (Auto-set)
# Set automatically when running in Docker
# DOCKER=false

# CI Environment Flag (Auto-set)
# Set automatically in CI/CD pipelines
# CI=false
